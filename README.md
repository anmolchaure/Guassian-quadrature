Numerical integration of f(x) = exp(x²) over [0.0, 1.5] using Gaussian quadrature (orders 1–6) and composite trapezoid method. 
Compares accuracy and function call count between both methods to determine the optimal quadrature order.
